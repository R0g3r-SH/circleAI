import { Box } from "@mui/material";
import React from "react";

const ComputerView = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      Vista de compu no se vale, hola roger, pon tu herramienta de inspeccion pa
      que pongas el cel view
    </Box>
  );
};

export default ComputerView;
